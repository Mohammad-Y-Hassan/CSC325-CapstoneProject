package com.example.capstoneproject;

import javafx.application.Application;
import javafx.fxml.FXML;
import javafx.scene.image.ImageView;
import javafx.stage.Stage;
import javafx.scene.control.TextField;

public class HomeViewController extends Application {
@FXML
private ImageView uploadPostButton;
//will insert a method here for when a user clicks on the upload post button
//screen will switch to the Uploadpostfxml file
    public static void main(String[] args) {
        launch(args);
    }

    @Override
    public void start(Stage primaryStage) {

    }
}
